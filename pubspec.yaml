name: flutter_web_demo
description: App built using Flutter

environment:
  sdk: ">=2.5.0-dev.4.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  built_collection: ^4.2.2
  built_value: ^6.7.1
  http: ^0.12.0+2

dev_dependencies:
  flutter_test:
      sdk: flutter
  build_runner: ^1.6.7
  build_web_compilers: ^2.2.3
  built_value_generator: ^6.7.1
  pedantic: ^1.8.0+1

flutter:
  uses-material-design: true
  assets:
    - icons/left_arrow.png